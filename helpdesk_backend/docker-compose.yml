version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: helpdesk-mysql
    restart: always
    environment:
      MYSQL_DATABASE: ${DB_NAME:helpdesk_db}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASS:root}
      MYSQL_USER: ${DB_USER:helpdesk_user}
      MYSQL_PASSWORD: ${DB_PASS:helpdesk_pass}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: